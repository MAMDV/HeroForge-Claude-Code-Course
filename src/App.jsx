function App() {
  return (
    <div
      className="mc-grain min-h-screen flex items-center justify-center transition-colors"
      style={{ backgroundColor: 'var(--lm-bg)', color: 'var(--lm-text)' }}
    >
      <main className="max-w-xl px-6 text-center">
        <h1 className="text-3xl font-semibold">LifeOps Command Center</h1>
        <p className="mt-3 opacity-80">
          Starter project — you'll build this out across the course workshops.
        </p>
      </main>
    </div>
  );
}

export default App;
